# progress.txt — 작업 진행 로그
#
# 기능 구현 세션마다 아래 형식으로 한 줄씩 추가한다.
# 형식 (세션마다 맨 위에 추가):
#
#   [YYYY-MM-DD HH:MM] <commit-hash> | <feature_id> <DONE|BLOCKED>
#     (1) 했다: 무엇을 어떤 방식으로 구현했는지 (설계 판단 포함)
#     (2) 주의: 막혔던 지점이나 다음 사람이 주의할 점
#     (3) 다음: 다음 세션에 무엇을 하면 좋을지와 그 이유
#
# 규칙:
#   - 최신 항목이 위로 온다.
#   - 완료 시 feature_list.json 의 해당 passes 를 true 로 바꾼다.
#   - 한 세션당 feature_id 하나를 원칙으로 한다.
#
# ---- 로그 (아래에 추가) ----
[2026-09-23 15:10] ea25ae8 | F-008 DONE
  (1) 했다: views/index.ejs 작업 칼럼에 `else if (item.status === 'rented')` 분기를 추가해
      <form class="return-form" method="post" action="/equipment/<id>/return"><button type="submit"
      class="btn btn--secondary">반납</button></form> 를 렌더링한다. 서버 코드는 바꾸지 않았다.
      설계 판단: 반납은 입력값이 없으므로 대여처럼 <details> 로 감싸지 않고 버튼이 바로 보이는 제출 폼으로 했다
      (UI_GUIDE §4 보조 버튼 = 반납). 조건을 else 가 아니라 명시적 'rented' 로 둔 것은 이후 overdue 가 status 가 아닌
      서버 판정값으로 들어올 때 의도치 않게 반납 버튼이 빠지거나 붙는 일을 막기 위해서다.
      경합 해결(F-007 세션 (2)의 숙제): playwright.config.js 에 workers: 1 을 넣어 전체를 직렬로 돌린다.
      파일별 serial 묶음은 파일 간 경합(F-006(a) 의 DB 스냅샷 vs F-008 의 상태 변경)을 못 막고, 전용 장비 추가는
      행 수를 DB COUNT 와 비교하는 F-003/F-004/F-007 과 경합한다. 전체 스위트가 약 2초라 비용이 없다.
      검증: e2e/F-008.spec.js — step 1 "대여 처리" 는 대여 라우트(F-011) 전이라 트랜잭션으로 rentals 에 미반납 행
      (renter 'F-008 테스트', due 2099-12-31)을 넣고 equipment.status='rented' 로 바꾼 뒤, DB 를 실제 쿼리해 status=rented,
      미반납 건 1개를 확인. step 2 는 해당 행의 .badge--rented "대여중", form.return-form method=post,
      action=/equipment/<id>/return, 버튼 visible/"반납"/.btn--secondary, 그 행엔 .rent-toggle/form.rent-form 0개,
      대여중이 아닌 행엔 form.return-form/"반납" 버튼 0개. finally 에서 rental 삭제+status 원복하고 원복도 쿼리로 확인.
      전체 npx playwright test 9/9 통과(1 worker). 회귀 확인: 조건을 'XXrented' 로 바꾸고 재기동 -> F-008 실패 -> 원복 -> 9/9.
      스크린샷 확인함 (desktop/mobile): id=3 을 rented 로 바꾼 상태로 test-results/ui-desktop.png(1280), ui-mobile.png(375)
      를 직접 열어봤다. Dell 행에만 "반납" 버튼, 나머지 5행은 "▶ 대여" 토글. 375px scrollWidth=375 로 가로 넘침 없음.
      촬영 후 id=3 을 available 로 복구하고 status<>'available' 0건, rentals 0건을 쿼리로 확인.
      발견한 문제: 기존과 같음 — app.css 가 없어 버튼이 브라우저 기본 스타일(U-002/U-005 몫), 한글 tofu 는 WSL 폰트 미설치.
      UI_GUIDE §6 에 F-008 선택자를 추가했다.
  (2) 주의: 반납 버튼을 누르면 POST /equipment/:id/return 라우트가 없어 404 다 — F-012 전까지의 과도 상태.
      workers: 1 을 되돌리면 F-006/F-008 이 다른 파일과 경합해 간헐 실패한다. 병렬이 필요해지면 테스트별 DB 파일
      (DB_PATH 환경변수 + 워커별 서버) 로 격리해야 한다.
      F-008 테스트가 만든 rentals 행은 finally 에서 지우지만, 테스트 프로세스가 강제 종료되면 남을 수 있다 — 이상하면 ./init.sh
      가 아니라(시드는 멱등이라 rentals 를 안 지움) rentals/status 를 직접 확인할 것.
      test-results/.last-run.json 이 여전히 git 추적 중이다(이번엔 내용이 안 바뀌어 dirty 되지 않았음). 추적 해제는 사람과 상의 필요.
  (3) 다음: F-009(대여 폼에 대여자명 입력). form.rent-form 안 제출 버튼 앞에 <label for="renter-<id>">대여자명</label>
      <input id="renter-<id>" name="renter_name" required> 를 넣으면 된다(id 는 행마다 유일해야 label 연결이 맞음).
      이어 F-010 은 같은 방식으로 type=date 의 due_date. 둘 다 끝나야 F-011(대여 POST 라우트) 가 입력값을 받을 수 있다.

[2026-09-23 14:40] 053a5d8 | F-007 DONE
  (1) 했다: views/index.ejs 에 thead "작업" th 와 행마다 <td class="equipment-actions"> 를 상태 칼럼 뒤에 추가했다.
      status === 'available' 인 행에만 <details class="rent-toggle"><summary class="btn btn--primary">대여</summary>
      <form class="rent-form" method="post" action="/equipment/<id>/rent"><button type="submit" class="btn btn--primary">대여</button>
      </form></details> 를 렌더링한다. 서버 코드는 바꾸지 않았다(status 는 이미 넘어옴).
      설계 판단: UI_GUIDE §4 "모달 vs 인라인 펼침 중 하나로 통일" 을 여기서 인라인 펼침(<details>)으로 확정했다 —
      JS 없이 열리고 제출되며(§2 SSR 우선), 모달 JS/포커스 관리가 필요 없다. 폼 action 은 REST 형태
      /equipment/:id/rent 로 정했다. 입력 필드(대여자명·반납예정일)는 F-009/F-010, POST 라우트는 F-011 몫이라 넣지 않았다.
      대여자/반납예정일 칼럼(F-022/F-023)도 없으므로 작업 칼럼은 지금 4번째다 — 그 칼럼들은 상태와 작업 사이에 끼워 넣을 것.
      검증: e2e/F-007.spec.js 에 steps 2개를 옮겼다 — (a) / 접속, #equipment-list 표시, 행 수 == DB COUNT(*)(실제 쿼리),
      "작업" 헤더. (b) 대여가능 배지가 있는 모든 행에 대해: summary "대여" 1개 보임, form.rent-form 이 method=post,
      action=/equipment/<그 행 id>/rent, 펼치기 전 숨김 -> summary 클릭 후 보이고 제출 버튼 "대여". 대여가능이 아닌 행엔
      .rent-toggle/form.rent-form 0개. 전체 npx playwright test 8/8 통과(smoke, F-002~F-007).
      회귀 확인: 조건을 sed 로 !== 'available' 로 뒤집고 재기동 -> F-007 실패 확인 -> sed 로 원복 -> 재기동 -> 8/8.
      수동 확인: DB 에서 id=6 을 rented 로 바꾸고 curl 로 해당 행에 rent-toggle 이 없고 나머지 5행에만 있음을 확인 ->
      available 로 복구(status<>'available' 0건 쿼리로 확인).
      스크린샷 확인함 (desktop/mobile): test-results/ui-desktop.png(1280), ui-mobile.png(375), 그리고 첫 행을 펼친
      ui-mobile-open.png 를 직접 열어봤다. 4칼럼 6행, 각 행 "▶ 대여" 토글, 펼치면 행 안에 제출 버튼이 보임. 375px 펼친 상태에서도
      scrollWidth=375 로 가로 넘침 없음. 발견한 문제: (a) app.css 가 없어 summary 가 삼각형 마커 붙은 평문, 버튼은 브라우저 기본
      스타일 — U-002/U-005 에서 .btn 과 details 마커 제거 스타일링 필요. (b) 한글 tofu(□)는 기존과 같은 원인(WSL 한글 폰트 미설치).
      UI_GUIDE §6 에 F-007 선택자를 추가했다.
  (2) 주의: 지금 제출 버튼을 누르면 POST 라우트가 없어 Express 기본 404("Cannot POST")가 난다 — F-011 전까지의 과도 상태다.
      F-007 테스트는 기대 행을 DB 가 아니라 렌더링된 배지(.badge--available) 기준으로 잡는다. F-006 (b) 가 병렬 실행 중
      공유 DB 의 status 를 잠깐 바꾸기 때문에 DB 기준으로 비교하면 경합으로 불안정해진다. 같은 이유로 이번에는 DB 를 바꾸는
      "대여중 행엔 대여 버튼 없음" 테스트를 추가하지 않고 수동 확인만 했다 — F-008 에서 대여중 행을 만들 때 격리 방식
      (파일 serial 묶음 또는 workers 조정, 혹은 전용 장비) 을 정하고 이 경우도 함께 검증할 것.
      summary 와 제출 버튼 텍스트가 둘 다 "대여"다. 테스트/선택자는 details.rent-toggle > summary 와 form.rent-form
      button[type=submit] 으로 구분할 것(getByRole('button', {name:'대여'}) 는 여러 개가 잡힌다).
      test-results/.last-run.json 이 git 에 추적되고 있어 테스트 실행마다 dirty 해진다(이번엔 checkout 으로 되돌려 커밋 제외).
      data/server.pid 처럼 추적 해제(git rm --cached) 를 사람과 상의해 별도 chore 커밋으로 처리하길 권한다.
  (3) 다음: F-008(대여중 행에 반납 버튼). 작업 칼럼의 else 분기에 <form class="return-form" method="post"
      action="/equipment/<id>/return"><button class="btn btn--secondary">반납</button></form> 를 두면 F-007 과 대칭이다.
      steps 1 이 "장비 하나를 대여 처리한다" 인데 대여 라우트(F-011) 전이라 F-006 처럼 DB 를 직접 바꾸되, 위 (2) 의 경합
      문제를 먼저 해결해야 한다. 이어 F-009/F-010 은 form.rent-form 안에 label for 연결된 input 두 개(type=date 포함)를 추가하면 된다.

[2026-09-23 14:10] 2952d84 | F-006 DONE
  (1) 했다: src/server.js 에 STATUS_LABELS = { available: '대여가능', rented: '대여중' } 를 두고 GET / 결과에
      statusLabel 을 붙여 넘긴다. views/index.ejs 에 thead "상태" th 와 행마다
      <td class="equipment-status"><span class="badge badge--<status>">라벨</span></td> 를 종류 칼럼 뒤에 추가했다.
      설계 판단: 매핑은 F-005 세션 권고대로 서버에 두었다 — 이후 연체(overdue)는 rentals.due_date 비교가 필요해
      서버에서 판정하는 게 자연스럽고, 그때 STATUS_LABELS 에 overdue: '연체' 를 추가하면 된다. 배지 클래스는
      DB 원문(badge--available/--rented)을 그대로 써서 UI_GUIDE §4 마크업과 일치시켰다. 스타일(app.css)은 U-003 범위라 만들지 않았다.
      검증: e2e/F-006.spec.js 에 steps 2개를 테스트 2건으로 옮겼다 — (a) 시드 상태: / 접속, #equipment-list 표시,
      "상태" 헤더, 행 수 == DB, data/app.db 실제 쿼리한 id 별 status 에 대해 셀 텍스트가 라벨과 정확히 일치,
      span.badge 1개 + badge--<status> 클래스, DB 원문(available/rented)이 셀에 노출되지 않음.
      (b) 대여중: 대여 기능(F-011) 전이라 DB 에서 첫 장비를 UPDATE status='rented' 로 바꾸고 같은 검증 +
      해당 행 .badge--rented 가 "대여중", 나머지는 "대여가능" 확인 -> finally 에서 원래 status 로 복구.
      전체 npx playwright test 7/7 통과(smoke, F-002~F-006). 회귀 확인: rented 라벨을 '대여가능'으로 일시 변경 ->
      재기동 -> (b) 실패 확인 -> 원복 -> 재기동 -> 7/7. 실패 경로에서도 DB 가 복구됨을 쿼리로 확인
      (status<>'available' 0건). UI_GUIDE §6 표에 F-006 선택자(thead th "상태", td.equipment-status,
      span.badge.badge--available/--rented)를 추가했다.
      스크린샷 확인함 (desktop/mobile): test-results/ui-desktop.png(1280), ui-mobile.png(375) 를 직접 열어봤다.
      3칼럼(장비명, 종류, 상태) 6행이 보이고 375px 에서 scrollWidth=375 로 가로 넘침 없음.
      발견한 문제: (a) app.css 가 없어 배지가 색 없는 평문으로 보인다 — U-003 에서 해결. (b) 한글 tofu(□)는
      F-005 기록과 같은 원인(WSL 한글 폰트 미설치). 앱 버그 아님.
  (2) 주의: F-006 (b) 테스트는 공유 DB(data/app.db)의 equipment.status 를 잠깐 바꾼다. 다른 파일과 병렬로 돌지만
      기존 테스트는 status 를 보지 않아 영향이 없다. 앞으로 status/버튼(F-007, F-008)을 검사하는 테스트를 추가하면
      이 테스트와 경합할 수 있으니, 그때는 해당 파일들을 test.describe.configure({ mode: 'serial' }) 로 묶거나
      상태를 바꾸는 테스트가 전용 장비를 쓰게 하는 등 격리 방식을 정할 것.
      td.equipment-status 셀 텍스트는 라벨 "만" 담는다고 가정한다(toHaveText 정확 일치) — 반납 버튼 등은 작업 칼럼에 둘 것.
      작업 중 실수: 회귀 확인 원복에 git checkout src/server.js 를 써서 미커밋 변경까지 되돌렸다(바로 재적용함).
      미커밋 상태에서 mutation 확인은 sed 로 되돌리거나 먼저 git stash/add 할 것.
  (3) 다음: F-007(대여가능 행에 대여 버튼/폼). 칼럼 순서(UI_GUIDE §4)상 작업 칼럼(오른쪽 정렬)에 .btn.btn--primary "대여"
      를 두고, 폼 방식(인페이지 모달 vs 행 아래 인라인 펼침)을 이 기능에서 하나로 확정해야 F-009/F-010 이 그 위에 쌓인다.
      JS 없이도 제출이 동작해야 하므로(UI_GUIDE §2) 행 아래 인라인 <form method="post"> 펼침(<details> 등)이 단순하다.
      대여중 행에는 대여 버튼이 없어야 하는지도 함께 검증하면 F-008 과 대칭이 된다.

[2026-09-23 13:40] 4592759 | F-005 DONE
  (1) 했다: views/index.ejs 에 thead "종류" th 와 행마다 <td class="equipment-type"><%= item.type %></td>
      를 장비명 칼럼 바로 뒤에 추가했다. GET / 쿼리가 이미 type 을 가져오고 있어서 서버 코드는 바꾸지 않았다.
      설계 판단: UI_GUIDE §4 칼럼 순서(장비명 -> 종류 -> 상태 -> ...)에 맞춰 두 번째 칼럼에 두었다. DB 에
      한글 값('노트북' 등)이 그대로 저장되어 있어서 매핑 없이 원문을 출력한다(F-006 의 status 와 다름).
      스타일(app.css)은 만들지 않았다 — U-002 범위다.
      검증: e2e/F-005.spec.js 에 steps 2개를 그대로 옮겼다 — (a) / 접속 후 #equipment-list 표시,
      (b) "종류" 헤더가 보이고, 행 수 == DB 장비 수, 모든 행이 td.equipment-type 을 정확히 1개 가지며
      값이 노트북/모니터/주변기기 중 하나, data/app.db 를 better-sqlite3 readonly 로 실제 쿼리한 id 별
      type 과 셀 텍스트가 정확히 일치, 세 종류가 모두 화면에 나타나는지 확인한다.
      전체 npx playwright test 5/5 통과(smoke, F-002~F-005). 회귀를 잡는지 보려고 템플릿을
      item.type.slice(0,2) 로 일시 변경 -> 재기동 -> F-005 실패 확인 -> 원복 -> 재기동 -> 5/5 통과.
      UI_GUIDE §6 표에 새 테스트 의존 선택자(thead th "종류", td.equipment-type)를 추가했다.
      스크린샷 확인함 (desktop/mobile): test-results/ui-desktop.png(1280), ui-mobile.png(375) 를 직접 열어봤다.
      2칼럼(장비명, 종류) 6행이 순서대로 보이고 375px 에서 가로 넘침 없음.
      발견한 문제: (a) app.css 가 아직 없어 브라우저 기본 스타일(serif 글꼴, 테두리 없는 표)이 그대로다
      — U-002/U-001 에서 해결할 부분이라 이번에는 손대지 않았다. (b) 스크린샷에서 한글이 모두 □(tofu)로
      보인다. WSL 에 한글 폰트가 설치되어 있지 않아서다(fc-list :lang=ko 결과 없음). 서버 HTML 은
      charset=utf-8 로 한글을 정상 전달하고(curl 확인) e2e 텍스트 검증도 통과하므로 앱 버그는 아니다.
  (2) 주의: 스크린샷 셀프 리뷰에서 한글을 눈으로 확인하려면 WSL 에 한글 폰트가 필요하다
      (예: sudo apt install fonts-noto-cjk — sudo 가 필요해서 이번 세션에서는 설치하지 않았다. 사람 판단 필요).
      td.equipment-type 셀도 type 원문 "만" 담는다고 가정한다(F-005 테스트가 toHaveText 로 정확 일치 검사).
      아이콘 등을 넣으려면 별도 요소로 분리할 것. 서버 핫리로드 없음 — 템플릿 수정 후 ./init.sh 재실행 필수.
  (3) 다음: F-006(상태 라벨). DB status 는 영문('available'/'rented')이라 한글 라벨 매핑이 필요하다.
      UI_GUIDE §4 가 마크업을 <span class="badge badge--available">대여가능</span> / badge--rented 대여중으로
      정해두었으므로 td.equipment-status 안에 이 배지를 렌더링하면 된다. 매핑은 서버(라우트에서 statusLabel
      을 붙여 넘김) 쪽을 권한다 — 이후 연체(overdue) 판정도 due_date 계산이 필요해서 서버에서 하는 게
      자연스럽다. 시드가 전부 available 이라 '대여중' 라벨 테스트는 대여 기능(F-011) 전까지 DB 에 rentals 를
      직접 넣는 방식이 필요할 수 있다 — 그 경우 테스트 후 원상복구해 다른 테스트와 격리할 것.

[2026-09-23 13:20] 089181a | F-004 DONE
  (1) 했다: 코드 변경 없이 검증만 추가했다. F-003 에서 views/index.ejs 가 이미 장비마다
      td.equipment-name 에 <%= item.name %> 를 출력하고 thead 에 "장비명" th 를 두고 있었기 때문이다.
      e2e/F-004.spec.js 는 steps 2개를 그대로 옮겼다 — (a) / 접속 후 #equipment-list 표시,
      (b) "장비명" 헤더가 보이고, 행 수 == DB 장비 수, 모든 행이 비어있지 않은 td.equipment-name 을
      정확히 1개 가지며, data/app.db 를 better-sqlite3 readonly 로 실제 쿼리한 장비 id 별 name 이
      해당 행 셀 텍스트와 정확히 일치(toHaveText 전체 일치)하는지 확인한다.
      설계 판단: F-003 테스트는 행에 이름이 "포함"되는지(toContainText)만 봤으므로, F-004 는 "장비명
      칼럼"에 "정확히" 표시되는지로 기준을 좁혔다. 시드 이름에 " 문자(MacBook Pro 14" 등)가 있어
      이스케이프가 깨지면 불일치로 잡힌다.
      검증: 전체 npx playwright test 4/4 통과(smoke, F-002, F-003, F-004). 테스트가 실제로 회귀를
      잡는지 보려고 템플릿의 장비명을 slice(0,3) 로 일시 변경 후 서버 재기동 -> F-004 실패 확인 ->
      원복 후 재기동 -> 4/4 통과를 확인했다(원복은 커밋 전 git status 로 확인, 템플릿 diff 없음).
  (2) 주의: F-004 테스트는 td.equipment-name 셀 텍스트가 name "만" 담고 있다고 가정한다. 이 셀에
      배지/아이콘/부가 텍스트를 넣으면 깨진다 — 부가 정보는 별도 td 로 추가할 것.
      서버는 여전히 핫리로드가 없다. 템플릿 수정 후 ./init.sh 재실행 필수.
  (3) 다음: F-005(종류 칼럼). 쿼리에 type 이 이미 있으므로 thead 에 "종류" th, 행에
      <td class="equipment-type"><%= item.type %></td> 를 추가하고, 테스트는 행마다 셀 텍스트가
      DB type 과 일치하며 노트북/모니터/주변기기 중 하나인지 확인하면 된다. 그 다음 F-006(상태 라벨,
      status 'available'->대여가능, 'rented'->대여중 매핑)은 DB 원문 값이 아닌 한글 라벨을 표시해야
      하므로 매핑 위치(서버 vs 템플릿)를 정해야 한다.

[2026-09-23 13:03] 55a8e65 | F-003 DONE
  (1) 했다: src/server.js 의 GET / 에서 SELECT id, name, type, status FROM equipment ORDER BY id 를
      동기 .all() 로 조회해 index 템플릿에 equipment 로 넘긴다. getDb() 는 호출마다 새 연결을 여는
      헬퍼라서 server.js 안에 conn() 으로 지연 생성한 연결 1개를 프로세스 동안 재사용하게 했다.
      views/index.ejs 는 #equipment-list 안에 <table><thead><tbody> 를 두고 장비마다
      <tr class="equipment-row" data-equipment-id="..."> 한 행을 렌더링한다.
      설계 판단: 시드 장비가 "나타나는지" 확인하려면 행을 식별할 내용이 필요해서 장비명 칼럼
      (td.equipment-name) 하나만 넣었다. 종류/상태 칼럼은 넣지 않았다(F-005/F-006 몫). 쿼리에는
      type/status 를 이미 포함했으므로 다음 기능들은 템플릿에 th/td 만 추가하면 된다.
      빈 목록 안내(U-008)와 스타일(U-002)은 손대지 않았다.
      검증: e2e/F-003.spec.js 는 steps 3개를 그대로 옮겼다 — (a) / 접속 후 #equipment-list 표시,
      (b) data/app.db 를 better-sqlite3 readonly 로 실제 쿼리해 노트북/모니터/주변기기 종류가 모두
      있는지, 장비 id 마다 행이 보이고 장비명이 들어있는지, (c) 행 수 == DB COUNT(*) (현재 6, 시드 개수
      이상인지도 확인). 전체 npx playwright test 3/3 통과(smoke, F-002 포함). 커밋 전 DB 를 따로
      쿼리해 equipment COUNT(*) = 6 도 확인했다.
  (2) 주의: F-003 테스트는 기대값을 DB 에서 가져오므로 대여 테스트가 추가돼도 깨지지 않지만, DB_PATH
      환경변수를 서버와 테스트에 다르게 주면 불일치한다(둘 다 기본값 data/app.db 사용).
      서버는 여전히 핫리로드가 없다 — 코드 수정 후 ./init.sh 재실행 필수.
      행 셀렉터 tr.equipment-row[data-equipment-id] 와 td.equipment-name 은 F-003 테스트가 의존하니
      이름을 바꾸지 말 것. 칼럼은 추가만 하면 호환된다.
      장비명은 <%= %> 로 이스케이프 출력 중이다(R-008 대비 <%- %> 로 바꾸지 말 것).
  (3) 다음: F-004(각 행에 장비명 표시). 이미 td.equipment-name 이 렌더링되므로 구현보다 검증 위주의
      세션이 될 것이다 — 모든 행에서 장비명 셀이 비어있지 않고 DB name 과 일치하는지 테스트한다.
      이어 F-005(종류 칼럼), F-006(상태 라벨: status 'available'->대여가능, 'rented'->대여중 매핑)을
      같은 행에 td 를 추가하는 방식으로 진행하면 된다.

[2026-09-23 12:57] 2fb3400 | F-002 DONE
  (1) 했다: views/index.ejs 를 새로 만들고 src/server.js 의 GET / 를 인라인 플레이스홀더 HTML 에서
      res.render('index', { title }) 로 바꿨다. 템플릿에는 h1("사내 장비 대여 관리")과 장비 목록 영역
      <section id="equipment-list" aria-labelledby=...>(접근성 region 이름 "장비 목록")만 둔다.
      설계 판단: 한 세션 한 기능 원칙에 따라 이번에는 DB 조회와 행 렌더링을 하지 않았다. 목록 영역은
      빈 section 이며, 테이블/행 구조는 F-003(시드 전체 표시)에서 정하는 것이 맞다고 봤다.
      검증: e2e/F-002.spec.js 에 steps 3개를 그대로 옮겼다 — (a) / 접속 200, (b) content-type text/html
      + title/h1 텍스트 + 스캐폴드 문구 부재(EJS 결과임을 확인), (c) #equipment-list 가 보이고
      role=region name="장비 목록" 으로 잡히는지. 전체 npx playwright test 2/2 통과(smoke 포함).
      DB 확인 단계는 이번 steps 에 없어 쿼리하지 않았다.
  (2) 주의: 서버는 핫리로드가 없다. 코드 수정 후 e2e 전에 ./init.sh 를 다시 돌려야 새 코드가 반영된다.
      data/server.pid 가 .gitignore 에 있음에도 git 에 추적되고 있어 init.sh 실행마다 작업 트리가
      dirty 해졌다(F-002 커밋에도 pid 변경이 섞여 들어감). 별도 커밋 a8a4d80 에서 git rm --cached 로
      추적 해제했으므로 이제는 init.sh 후에도 트리가 깨끗해야 한다.
      F-003 테스트에서 행을 셀 때 #equipment-list 내부를 기준으로 잡으면 이번 테스트와 호환된다.
      id/aria 이름을 바꾸면 F-002 테스트가 깨지니 유지할 것.
  (3) 다음: F-003(시드 장비 6개 전체 표시). GET / 에서 getDb() 로 equipment 를 동기 조회(async 금지)해
      템플릿에 넘기고 #equipment-list 안에 행을 렌더링한다. 시드 개수(6, scripts/init-db.js 의
      SEED_EQUIPMENT)와 행 수 일치를 테스트하되, 이후 대여 테스트가 DB 를 바꿀 수 있으니 기대값은
      DB COUNT(*) 쿼리로 구하는 편이 안전하다. 이어서 F-004~F-006 은 같은 행에 이름/종류/상태 칼럼을
      더하는 작업이라 F-003 의 행 구조를 확장 가능하게 잡는 것이 좋다.

[2026-09-23 11:05] F-001 DONE - 서버 기동 및 /health 헬스체크 정상 응답 확인, feature_list.json passes:true 처리

(1) 무엇을 어떤 방식으로 구현했는지:
    - 이번 세션 시작 시점에 feature_list.json 의 모든 항목이 passes:false 상태였고, F-001("서버가
      기동되고 /health 엔드포인트가 정상 응답한다")이 최우선 순위였다.
    - 실제로 확인해보니 이전 스캐폴드 세션(35536fc)에서 src/server.js 에 /health 라우트가 이미
      구현되어 있었다 ({ status: 'ok', db, ts } 반환). 별도 코드 구현 없이, ./init.sh 를 재실행해
      의존성 설치 -> DB 초기화 -> 서버 기동 -> 헬스체크(curl)까지 전 과정이 정상 동작함을 검증하고,
      curl http://localhost:3000/health 응답에 status:"ok" 포함을 재확인한 뒤 F-001.passes 만 true 로
      변경했다.
    - init.sh 에 이미 존재하던 미커밋 변경(WSL/Linux 플랫폼 가드 추가, 실행권한 부여)은 브라우저
      환경 안전성과 직접 관련되어 있어 함께 커밋에 포함시켰다. feature 구현과 무관한 로직 변경은
      아니므로 별도 커밋 분리는 하지 않았다.
    - 루트(/) 는 아직 스캐폴드 플레이스홀더 HTML을 반환하는 상태를 그대로 두었다(F-002 이후 담당).

(2) 막혔던 지점이나 다음 사람이 주의할 점:
    - 이 세션에는 브라우저 자동화 도구(Playwright 등)가 연결되어 있지 않아 "브라우저로 접속 확인"을
      curl 요청으로 대체 검증했다. 실제 브라우저 렌더링 확인이 필요한 UI 계열 항목(U-*)을 다룰 때는
      이 점을 감안해 별도 확인 수단을 마련해야 한다.
    - views/, public/ 디렉터리는 아직 비어있다(.gitkeep 만 존재). EJS 템플릿이 전혀 없으므로
      F-002(루트 페이지 EJS 렌더링)부터 실질적인 뷰 작업이 시작된다.
    - data/ 디렉터리에는 .gitignore 로 걸러지지 않는 data/server.pid, data/server.log 가 남아있어
      git status 에 untracked 로 계속 잡힌다(기능 동작에는 영향 없음, 필요시 .gitignore 보강 검토).

(3) 다음 세션에 무엇을 하면 좋을지와 그 이유:
    - 다음 최우선 항목은 F-002(루트 페이지 EJS 렌더링)다. 현재 라우트가 인라인 HTML 문자열을
      반환하고 있으므로, views/index.ejs 를 만들고 equipment 테이블을 조회해 장비 목록을 렌더링하는
      기본 뼈대부터 잡아야 한다. 이후 F-003~F-006(목록/이름/종류/상태 표시)이 자연스럽게 이어진다.
    - 대여/반납 트랜잭션(F-011~F-018), 연체 판정(F-019~F-021), 에러 처리(F-013~F-015, F-024~F-026)는
      스키마(rentals.returned_at, due_date)가 이미 준비되어 있으므로 라우트/뷰 작업만 남아있다.
